export enum LogEventType {
  OTHER = 'OTHER',
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  RESTORE = 'RESTORE'
}

export enum FormulaInputType {
  MONETARY = 1,
  PERCENTAGE = 2
}

export enum FieldType {
  SHORT_STRING = 1,
  LONG_STRING = 2
}
